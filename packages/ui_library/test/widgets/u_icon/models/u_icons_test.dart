import 'package:flutter_test/flutter_test.dart';
import 'package:ui_library/widgets/u_icon/u_icon_export.dart';

void main() {
  const _kIconsFontFamily = 'UIcons';
  const String? _kIconsFontPackage = null;
  test('Test all UIcons', () {
    const aboutIcon = UIcons.about;
    expect(
      aboutIcon,
      const UIconData(0xe800,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const addButton = UIcons.add_button;
    expect(
      addButton,
      const UIconData(0xe801,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const addContactMemberIcon = UIcons.add_contact_member;
    expect(
      addContactMemberIcon,
      const UIconData(0xe802,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const addFolderIcon = UIcons.add_folder;
    expect(
      addFolderIcon,
      const UIconData(0xe803,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const addNoteIcon = UIcons.add_note;
    expect(
      addNoteIcon,
      const UIconData(0xe804,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const backArrowButton = UIcons.back_arrow_button;
    expect(
      backArrowButton,
      const UIconData(0xe805,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const callIcon = UIcons.call;
    expect(
      callIcon,
      const UIconData(0xe806,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const cameraIcon = UIcons.camera;
    expect(
      cameraIcon,
      const UIconData(0xe807,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const chatbarEmojis = UIcons.chatbar_emojis;
    expect(
      chatbarEmojis,
      const UIconData(0xe809,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const chatbarGifs = UIcons.chatbar_gifs;
    expect(
      chatbarGifs,
      const UIconData(0xe808,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const chatbarSendButton = UIcons.chatbar_send_button;
    expect(
      chatbarSendButton,
      const UIconData(0xe80a,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const chatbarStickers = UIcons.chatbar_stickers;
    expect(
      chatbarStickers,
      const UIconData(0xe80b,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const checkmarkIcon = UIcons.checkmark;
    expect(
      checkmarkIcon,
      const UIconData(0xe80c,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const closeButton = UIcons.close_button;
    expect(
      closeButton,
      const UIconData(0xe80d,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const composeMessageButton = UIcons.compose_message_button;
    expect(
      composeMessageButton,
      const UIconData(0xe80e,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const copyCloneButton = UIcons.copy_clone_button;
    expect(
      copyCloneButton,
      const UIconData(0xe80f,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const endCallIcon = UIcons.end_call;
    expect(
      endCallIcon,
      const UIconData(0xe810,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const endVideoCallIcon = UIcons.end_video_call;
    expect(
      endVideoCallIcon,
      const UIconData(0xe811,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const favoriteButton = UIcons.favorite_button;
    expect(
      favoriteButton,
      const UIconData(0xe812,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const fileIcon = UIcons.file;
    expect(
      fileIcon,
      const UIconData(0xe813,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const forwardArrowButton = UIcons.forward_arrow_button;
    expect(
      forwardArrowButton,
      const UIconData(0xe814,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const friendAddedIcon = UIcons.friend_added;
    expect(
      friendAddedIcon,
      const UIconData(0xe815,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const gridView = UIcons.grid_view;
    expect(
      gridView,
      const UIconData(0xe816,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const hamburgerMenu = UIcons.hamburger_menu;
    expect(
      hamburgerMenu,
      const UIconData(0xe817,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const lefthandNavigationDrawer = UIcons.lefthand_navigation_drawer;
    expect(
      lefthandNavigationDrawer,
      const UIconData(0xe818,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const linkButton = UIcons.link_button;
    expect(
      linkButton,
      const UIconData(0xe819,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const listView = UIcons.list_view;
    expect(
      listView,
      const UIconData(0xe81a,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const locationIcon = UIcons.location;
    expect(
      locationIcon,
      const UIconData(0xe81b,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const menuBarContacts = UIcons.menu_bar_contacts;
    expect(
      menuBarContacts,
      const UIconData(0xe81c,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const menuBarFiles = UIcons.menu_bar_files;
    expect(
      menuBarFiles,
      const UIconData(0xe81d,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const menuBarHome = UIcons.menu_bar_home;
    expect(
      menuBarHome,
      const UIconData(0xe81e,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const menuBarNotifications = UIcons.menu_bar_notifications;
    expect(
      menuBarNotifications,
      const UIconData(0xe81f,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const messageIcon = UIcons.message;
    expect(
      messageIcon,
      const UIconData(0xe820,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const multipleAccountsIcon = UIcons.multiple_accounts;
    expect(
      multipleAccountsIcon,
      const UIconData(0xe821,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const muteIcon = UIcons.mute;
    expect(
      muteIcon,
      const UIconData(0xe822,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const pinEntryBackspaceButton = UIcons.pin_entry_backspace_button;
    expect(
      pinEntryBackspaceButton,
      const UIconData(0xe823,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const qrCodeIcon = UIcons.qr_code;
    expect(
      qrCodeIcon,
      const UIconData(0xe824,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const removeFriendIcon = UIcons.remove_friend;
    expect(
      removeFriendIcon,
      const UIconData(0xe825,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const removeIcon = UIcons.remove;
    expect(
      removeIcon,
      const UIconData(0xe826,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const rightArrowInlineButton = UIcons.right_arrow_inline_button;
    expect(
      rightArrowInlineButton,
      const UIconData(0xe827,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const saveButton = UIcons.save_button;
    expect(
      saveButton,
      const UIconData(0xe828,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const searchIcon = UIcons.search;
    expect(
      searchIcon,
      const UIconData(0xe829,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const settingIcon = UIcons.setting;
    expect(
      settingIcon,
      const UIconData(0xe82a,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const settingsLogoutIcon = UIcons.settings_logout;
    expect(
      settingsLogoutIcon,
      const UIconData(0xe82b,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const swapCameraIcon = UIcons.swap_camera;
    expect(
      swapCameraIcon,
      const UIconData(0xe82c,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const tapSelectBox = UIcons.tap_select_box;
    expect(
      tapSelectBox,
      const UIconData(0xe82d,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const unmuteIcon = UIcons.unmute;
    expect(
      unmuteIcon,
      const UIconData(0xe82e,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const vectorStroke = UIcons.vector_stroke;
    expect(
      vectorStroke,
      const UIconData(0xe82f,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );

    const videoCallButton = UIcons.video_call_button;
    expect(
      videoCallButton,
      const UIconData(0xe830,
          uFontFamily: _kIconsFontFamily, uFontPackage: _kIconsFontPackage),
    );
  });
}
