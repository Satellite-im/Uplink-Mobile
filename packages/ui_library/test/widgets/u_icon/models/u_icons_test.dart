// ignore_for_file: constant_identifier_names

import 'package:flutter_test/flutter_test.dart';
import 'package:ui_library/widgets/u_icon/u_icon_export.dart';

void main() {
  const _kIconsFontFamily = 'UIcons';
  test('Test all UIcons', () {
    const about = UIcons.about;
    expect(
      about,
      const UIconData(0xe800, uFontFamily: _kIconsFontFamily),
    );

    const add_button = UIcons.add_button;
    expect(
      add_button,
      const UIconData(0xe801, uFontFamily: _kIconsFontFamily),
    );

    const add_contact_member = UIcons.add_contact_member;
    expect(
      add_contact_member,
      const UIconData(0xe802, uFontFamily: _kIconsFontFamily),
    );

    const add_folder = UIcons.add_folder;
    expect(
      add_folder,
      const UIconData(0xe803, uFontFamily: _kIconsFontFamily),
    );

    const add_note = UIcons.add_note;
    expect(
      add_note,
      const UIconData(0xe804, uFontFamily: _kIconsFontFamily),
    );

    const apple_watch = UIcons.apple_watch;
    expect(
      apple_watch,
      const UIconData(0xe805, uFontFamily: _kIconsFontFamily),
    );

    const audio_off = UIcons.audio_off;
    expect(
      audio_off,
      const UIconData(0xe806, uFontFamily: _kIconsFontFamily),
    );

    const audio_on = UIcons.audio_on;
    expect(
      audio_on,
      const UIconData(0xe807, uFontFamily: _kIconsFontFamily),
    );

    const back_arrow_button = UIcons.back_arrow_button;
    expect(
      back_arrow_button,
      const UIconData(0xe808, uFontFamily: _kIconsFontFamily),
    );

    const call = UIcons.call;
    expect(
      call,
      const UIconData(0xe809, uFontFamily: _kIconsFontFamily),
    );

    const camera = UIcons.camera;
    expect(
      camera,
      const UIconData(0xe80a, uFontFamily: _kIconsFontFamily),
    );

    const chatbar_emojis = UIcons.chatbar_emojis;
    expect(
      chatbar_emojis,
      const UIconData(0xe80b, uFontFamily: _kIconsFontFamily),
    );

    const chatbar_gifs = UIcons.chatbar_gifs;
    expect(
      chatbar_gifs,
      const UIconData(0xe80c, uFontFamily: _kIconsFontFamily),
    );

    const chatbar_send_button = UIcons.chatbar_send_button;
    expect(
      chatbar_send_button,
      const UIconData(0xe80d, uFontFamily: _kIconsFontFamily),
    );

    const chatbar_stickers = UIcons.chatbar_stickers;
    expect(
      chatbar_stickers,
      const UIconData(0xe80e, uFontFamily: _kIconsFontFamily),
    );

    const checkmark_rounded = UIcons.checkmark_rounded;
    expect(
      checkmark_rounded,
      const UIconData(0xe80f, uFontFamily: _kIconsFontFamily),
    );

    const checkmark = UIcons.checkmark;
    expect(
      checkmark,
      const UIconData(0xe810, uFontFamily: _kIconsFontFamily),
    );

    const close_button = UIcons.close_button;
    expect(
      close_button,
      const UIconData(0xe811, uFontFamily: _kIconsFontFamily),
    );

    const compose_message_button = UIcons.compose_message_button;
    expect(
      compose_message_button,
      const UIconData(0xe812, uFontFamily: _kIconsFontFamily),
    );

    const copy_clone_button = UIcons.copy_clone_button;
    expect(
      copy_clone_button,
      const UIconData(0xe813, uFontFamily: _kIconsFontFamily),
    );

    const desktop = UIcons.desktop;
    expect(
      desktop,
      const UIconData(0xe814, uFontFamily: _kIconsFontFamily),
    );

    const developer = UIcons.developer;
    expect(
      developer,
      const UIconData(0xe815, uFontFamily: _kIconsFontFamily),
    );

    const devices = UIcons.devices;
    expect(
      devices,
      const UIconData(0xe816, uFontFamily: _kIconsFontFamily),
    );

    const dropdown_menu = UIcons.dropdown_menu;
    expect(
      dropdown_menu,
      const UIconData(0xe817, uFontFamily: _kIconsFontFamily),
    );

    const encrypted = UIcons.encrypted;
    expect(
      encrypted,
      const UIconData(0xe818, uFontFamily: _kIconsFontFamily),
    );

    const end_call = UIcons.end_call;
    expect(
      end_call,
      const UIconData(0xe819, uFontFamily: _kIconsFontFamily),
    );

    const end_video_call = UIcons.end_video_call;
    expect(
      end_video_call,
      const UIconData(0xe81a, uFontFamily: _kIconsFontFamily),
    );

    const favorite_button = UIcons.favorite_button;
    expect(
      favorite_button,
      const UIconData(0xe81b, uFontFamily: _kIconsFontFamily),
    );

    const file = UIcons.file;
    expect(
      file,
      const UIconData(0xe81c, uFontFamily: _kIconsFontFamily),
    );

    const forward_arrow_button = UIcons.forward_arrow_button;
    expect(
      forward_arrow_button,
      const UIconData(0xe81d, uFontFamily: _kIconsFontFamily),
    );

    const friend_added = UIcons.friend_added;
    expect(
      friend_added,
      const UIconData(0xe81e, uFontFamily: _kIconsFontFamily),
    );

    const general = UIcons.general;
    expect(
      general,
      const UIconData(0xe81f, uFontFamily: _kIconsFontFamily),
    );

    const grid_view = UIcons.grid_view;
    expect(
      grid_view,
      const UIconData(0xe820, uFontFamily: _kIconsFontFamily),
    );

    const hamburger_menu = UIcons.hamburger_menu;
    expect(
      hamburger_menu,
      const UIconData(0xe821, uFontFamily: _kIconsFontFamily),
    );

    const hide = UIcons.hide;
    expect(
      hide,
      const UIconData(0xe822, uFontFamily: _kIconsFontFamily),
    );

    const image = UIcons.image;
    expect(
      image,
      const UIconData(0xe823, uFontFamily: _kIconsFontFamily),
    );

    const import = UIcons.import;
    expect(
      import,
      const UIconData(0xe824, uFontFamily: _kIconsFontFamily),
    );

    const laptop = UIcons.laptop;
    expect(
      laptop,
      const UIconData(0xe825, uFontFamily: _kIconsFontFamily),
    );

    const lefthand_navigation_drawer = UIcons.lefthand_navigation_drawer;
    expect(
      lefthand_navigation_drawer,
      const UIconData(0xe826, uFontFamily: _kIconsFontFamily),
    );

    const link_button = UIcons.link_button;
    expect(
      link_button,
      const UIconData(0xe827, uFontFamily: _kIconsFontFamily),
    );

    const list_view = UIcons.list_view;
    expect(
      list_view,
      const UIconData(0xe828, uFontFamily: _kIconsFontFamily),
    );

    const location = UIcons.location;
    expect(
      location,
      const UIconData(0xe829, uFontFamily: _kIconsFontFamily),
    );

    const menu_bar_contacts = UIcons.menu_bar_contacts;
    expect(
      menu_bar_contacts,
      const UIconData(0xe82a, uFontFamily: _kIconsFontFamily),
    );

    const menu_bar_files = UIcons.menu_bar_files;
    expect(
      menu_bar_files,
      const UIconData(0xe82b, uFontFamily: _kIconsFontFamily),
    );

    const menu_bar_home = UIcons.menu_bar_home;
    expect(
      menu_bar_home,
      const UIconData(0xe82c, uFontFamily: _kIconsFontFamily),
    );

    const menu_bar_notifications = UIcons.menu_bar_notifications;
    expect(
      menu_bar_notifications,
      const UIconData(0xe82d, uFontFamily: _kIconsFontFamily),
    );

    const message = UIcons.message;
    expect(
      message,
      const UIconData(0xe82e, uFontFamily: _kIconsFontFamily),
    );

    const mobile_phone = UIcons.mobile_phone;
    expect(
      mobile_phone,
      const UIconData(0xe82f, uFontFamily: _kIconsFontFamily),
    );

    const multiple_accounts = UIcons.multiple_accounts;
    expect(
      multiple_accounts,
      const UIconData(0xe830, uFontFamily: _kIconsFontFamily),
    );

    const mute = UIcons.mute;
    expect(
      mute,
      const UIconData(0xe831, uFontFamily: _kIconsFontFamily),
    );

    const pin_entry_backspace_button = UIcons.pin_entry_backspace_button;
    expect(
      pin_entry_backspace_button,
      const UIconData(0xe832, uFontFamily: _kIconsFontFamily),
    );

    const pinned_message_button = UIcons.pinned_message_button;
    expect(
      pinned_message_button,
      const UIconData(0xe833, uFontFamily: _kIconsFontFamily),
    );

    const qr_code_scanner_border = UIcons.qr_code_scanner_border;
    expect(
      qr_code_scanner_border,
      const UIconData(0xe834, uFontFamily: _kIconsFontFamily),
    );

    const qr_code = UIcons.qr_code;
    expect(
      qr_code,
      const UIconData(0xe835, uFontFamily: _kIconsFontFamily),
    );

    const refresh_try_again = UIcons.refresh_try_again;
    expect(
      refresh_try_again,
      const UIconData(0xe836, uFontFamily: _kIconsFontFamily),
    );

    const remove_friend = UIcons.remove_friend;
    expect(
      remove_friend,
      const UIconData(0xe837, uFontFamily: _kIconsFontFamily),
    );

    const remove = UIcons.remove;
    expect(
      remove,
      const UIconData(0xe838, uFontFamily: _kIconsFontFamily),
    );

    const reveal = UIcons.reveal;
    expect(
      reveal,
      const UIconData(0xe839, uFontFamily: _kIconsFontFamily),
    );

    const right_arrow_inline_button = UIcons.right_arrow_inline_button;
    expect(
      right_arrow_inline_button,
      const UIconData(0xe83a, uFontFamily: _kIconsFontFamily),
    );

    const save_button = UIcons.save_button;
    expect(
      save_button,
      const UIconData(0xe83b, uFontFamily: _kIconsFontFamily),
    );

    const search = UIcons.search;
    expect(
      search,
      const UIconData(0xe83c, uFontFamily: _kIconsFontFamily),
    );

    const security = UIcons.security;
    expect(
      security,
      const UIconData(0xe83d, uFontFamily: _kIconsFontFamily),
    );

    const setting = UIcons.setting;
    expect(
      setting,
      const UIconData(0xe83e, uFontFamily: _kIconsFontFamily),
    );

    const settings_logout = UIcons.settings_logout;
    expect(
      settings_logout,
      const UIconData(0xe83f, uFontFamily: _kIconsFontFamily),
    );

    const swap_camera = UIcons.swap_camera;
    expect(
      swap_camera,
      const UIconData(0xe840, uFontFamily: _kIconsFontFamily),
    );

    const tablet = UIcons.tablet;
    expect(
      tablet,
      const UIconData(0xe841, uFontFamily: _kIconsFontFamily),
    );

    const tap_select_box = UIcons.tap_select_box;
    expect(
      tap_select_box,
      const UIconData(0xe842, uFontFamily: _kIconsFontFamily),
    );

    const unencrypted = UIcons.unencrypted;
    expect(
      unencrypted,
      const UIconData(0xe843, uFontFamily: _kIconsFontFamily),
    );

    const unmute = UIcons.unmute;
    expect(
      unmute,
      const UIconData(0xe844, uFontFamily: _kIconsFontFamily),
    );

    const video_call_button = UIcons.video_call_button;
    expect(
      video_call_button,
      const UIconData(0xe845, uFontFamily: _kIconsFontFamily),
    );
  });
}
