// ignore_for_file: constant_identifier_names

import 'package:flutter_test/flutter_test.dart';
import 'package:ui_library/widgets/u_icon/u_icon_export.dart';

void main() {
  const _kIconsFontFamily = 'UIcons';
  test('Test all UIcons', () {
    const UIconData about = UIcons.about;
    expect(
      about,
      const UIconData(0xe900, uFontFamily: _kIconsFontFamily),
    );
    const UIconData add_button = UIcons.add_button;
    expect(
      add_button,
      const UIconData(0xe901, uFontFamily: _kIconsFontFamily),
    );
    const UIconData add_contact_member = UIcons.add_contact_member;
    expect(
      add_contact_member,
      const UIconData(0xe902, uFontFamily: _kIconsFontFamily),
    );
    const UIconData add_folder = UIcons.add_folder;
    expect(
      add_folder,
      const UIconData(0xe903, uFontFamily: _kIconsFontFamily),
    );
    const UIconData add_note = UIcons.add_note;
    expect(
      add_note,
      const UIconData(0xe904, uFontFamily: _kIconsFontFamily),
    );
    const UIconData apple_watch = UIcons.apple_watch;
    expect(
      apple_watch,
      const UIconData(0xe905, uFontFamily: _kIconsFontFamily),
    );
    const UIconData audio_off = UIcons.audio_off;
    expect(
      audio_off,
      const UIconData(0xe906, uFontFamily: _kIconsFontFamily),
    );
    const UIconData audio_on = UIcons.audio_on;
    expect(
      audio_on,
      const UIconData(0xe907, uFontFamily: _kIconsFontFamily),
    );
    const UIconData back_arrow_button = UIcons.back_arrow_button;
    expect(
      back_arrow_button,
      const UIconData(0xe908, uFontFamily: _kIconsFontFamily),
    );
    const UIconData call = UIcons.call;
    expect(
      call,
      const UIconData(0xe909, uFontFamily: _kIconsFontFamily),
    );
    const UIconData camera = UIcons.camera;
    expect(
      camera,
      const UIconData(0xe90a, uFontFamily: _kIconsFontFamily),
    );
    const UIconData chatbar_emojis = UIcons.chatbar_emojis;
    expect(
      chatbar_emojis,
      const UIconData(0xe90b, uFontFamily: _kIconsFontFamily),
    );
    const UIconData chatbar_gifs = UIcons.chatbar_gifs;
    expect(
      chatbar_gifs,
      const UIconData(0xe90c, uFontFamily: _kIconsFontFamily),
    );
    const UIconData chatbar_send_button = UIcons.chatbar_send_button;
    expect(
      chatbar_send_button,
      const UIconData(0xe90d, uFontFamily: _kIconsFontFamily),
    );
    const UIconData chatbar_stickers = UIcons.chatbar_stickers;
    expect(
      chatbar_stickers,
      const UIconData(0xe90e, uFontFamily: _kIconsFontFamily),
    );
    const UIconData checkmark_rounded = UIcons.checkmark_rounded;
    expect(
      checkmark_rounded,
      const UIconData(0xe90f, uFontFamily: _kIconsFontFamily),
    );
    const UIconData checkmark = UIcons.checkmark;
    expect(
      checkmark,
      const UIconData(0xe910, uFontFamily: _kIconsFontFamily),
    );
    const UIconData close_button = UIcons.close_button;
    expect(
      close_button,
      const UIconData(0xe911, uFontFamily: _kIconsFontFamily),
    );
    const UIconData compose_message_button = UIcons.compose_message_button;
    expect(
      compose_message_button,
      const UIconData(0xe912, uFontFamily: _kIconsFontFamily),
    );
    const UIconData copy_clone_button = UIcons.copy_clone_button;
    expect(
      copy_clone_button,
      const UIconData(0xe913, uFontFamily: _kIconsFontFamily),
    );
    const UIconData desktop = UIcons.desktop;
    expect(
      desktop,
      const UIconData(0xe914, uFontFamily: _kIconsFontFamily),
    );
    const UIconData developer = UIcons.developer;
    expect(
      developer,
      const UIconData(0xe915, uFontFamily: _kIconsFontFamily),
    );
    const UIconData devices = UIcons.devices;
    expect(
      devices,
      const UIconData(0xe916, uFontFamily: _kIconsFontFamily),
    );
    const UIconData dropdown_menu = UIcons.dropdown_menu;
    expect(
      dropdown_menu,
      const UIconData(0xe917, uFontFamily: _kIconsFontFamily),
    );
    const UIconData encrypted = UIcons.encrypted;
    expect(
      encrypted,
      const UIconData(0xe918, uFontFamily: _kIconsFontFamily),
    );
    const UIconData end_call = UIcons.end_call;
    expect(
      end_call,
      const UIconData(0xe919, uFontFamily: _kIconsFontFamily),
    );
    const UIconData end_video_call = UIcons.end_video_call;
    expect(
      end_video_call,
      const UIconData(0xe91a, uFontFamily: _kIconsFontFamily),
    );
    const UIconData favorite_button = UIcons.favorite_button;
    expect(
      favorite_button,
      const UIconData(0xe91b, uFontFamily: _kIconsFontFamily),
    );
    const UIconData file = UIcons.file;
    expect(
      file,
      const UIconData(0xe91c, uFontFamily: _kIconsFontFamily),
    );
    const UIconData forward_arrow_button = UIcons.forward_arrow_button;
    expect(
      forward_arrow_button,
      const UIconData(0xe91d, uFontFamily: _kIconsFontFamily),
    );
    const UIconData friend_added = UIcons.friend_added;
    expect(
      friend_added,
      const UIconData(0xe91e, uFontFamily: _kIconsFontFamily),
    );
    const UIconData general = UIcons.general;
    expect(
      general,
      const UIconData(0xe91f, uFontFamily: _kIconsFontFamily),
    );
    const UIconData go_back = UIcons.go_back;
    expect(
      go_back,
      const UIconData(0xe946, uFontFamily: _kIconsFontFamily),
    );
    const UIconData grid_view = UIcons.grid_view;
    expect(
      grid_view,
      const UIconData(0xe920, uFontFamily: _kIconsFontFamily),
    );
    const UIconData hamburger_menu = UIcons.hamburger_menu;
    expect(
      hamburger_menu,
      const UIconData(0xe921, uFontFamily: _kIconsFontFamily),
    );
    const UIconData hide = UIcons.hide;
    expect(
      hide,
      const UIconData(0xe922, uFontFamily: _kIconsFontFamily),
    );
    const UIconData image = UIcons.image;
    expect(
      image,
      const UIconData(0xe923, uFontFamily: _kIconsFontFamily),
    );
    const UIconData import = UIcons.import;
    expect(
      import,
      const UIconData(0xe924, uFontFamily: _kIconsFontFamily),
    );
    const UIconData laptop = UIcons.laptop;
    expect(
      laptop,
      const UIconData(0xe925, uFontFamily: _kIconsFontFamily),
    );
    const UIconData lefthand_navigation_drawer =
        UIcons.lefthand_navigation_drawer;
    expect(
      lefthand_navigation_drawer,
      const UIconData(0xe926, uFontFamily: _kIconsFontFamily),
    );
    const UIconData link_button = UIcons.link_button;
    expect(
      link_button,
      const UIconData(0xe927, uFontFamily: _kIconsFontFamily),
    );
    const UIconData list_view = UIcons.list_view;
    expect(
      list_view,
      const UIconData(0xe928, uFontFamily: _kIconsFontFamily),
    );
    const UIconData location = UIcons.location;
    expect(
      location,
      const UIconData(0xe929, uFontFamily: _kIconsFontFamily),
    );
    const UIconData menu_bar_contacts = UIcons.menu_bar_contacts;
    expect(
      menu_bar_contacts,
      const UIconData(0xe92a, uFontFamily: _kIconsFontFamily),
    );
    const UIconData menu_bar_files = UIcons.menu_bar_files;
    expect(
      menu_bar_files,
      const UIconData(0xe92b, uFontFamily: _kIconsFontFamily),
    );
    const UIconData menu_bar_home = UIcons.menu_bar_home;
    expect(
      menu_bar_home,
      const UIconData(0xe92c, uFontFamily: _kIconsFontFamily),
    );
    const UIconData menu_bar_notifications = UIcons.menu_bar_notifications;
    expect(
      menu_bar_notifications,
      const UIconData(0xe92d, uFontFamily: _kIconsFontFamily),
    );
    const UIconData message = UIcons.message;
    expect(
      message,
      const UIconData(0xe92e, uFontFamily: _kIconsFontFamily),
    );
    const UIconData mobile_phone = UIcons.mobile_phone;
    expect(
      mobile_phone,
      const UIconData(0xe92f, uFontFamily: _kIconsFontFamily),
    );
    const UIconData multiple_accounts = UIcons.multiple_accounts;
    expect(
      multiple_accounts,
      const UIconData(0xe930, uFontFamily: _kIconsFontFamily),
    );
    const UIconData mute = UIcons.mute;
    expect(
      mute,
      const UIconData(0xe931, uFontFamily: _kIconsFontFamily),
    );
    const UIconData pin_entry_backspace_button =
        UIcons.pin_entry_backspace_button;
    expect(
      pin_entry_backspace_button,
      const UIconData(0xe932, uFontFamily: _kIconsFontFamily),
    );
    const UIconData pinned_message_button = UIcons.pinned_message_button;
    expect(
      pinned_message_button,
      const UIconData(0xe933, uFontFamily: _kIconsFontFamily),
    );
    const UIconData qr_code = UIcons.qr_code;
    expect(
      qr_code,
      const UIconData(0xe934, uFontFamily: _kIconsFontFamily),
    );
    const UIconData qr_code_scanner_border = UIcons.qr_code_scanner_border;
    expect(
      qr_code_scanner_border,
      const UIconData(0xe935, uFontFamily: _kIconsFontFamily),
    );
    const UIconData refresh_try_again = UIcons.refresh_try_again;
    expect(
      refresh_try_again,
      const UIconData(0xe936, uFontFamily: _kIconsFontFamily),
    );
    const UIconData remove_friend = UIcons.remove_friend;
    expect(
      remove_friend,
      const UIconData(0xe937, uFontFamily: _kIconsFontFamily),
    );
    const UIconData remove = UIcons.remove;
    expect(
      remove,
      const UIconData(0xe938, uFontFamily: _kIconsFontFamily),
    );
    const UIconData reveal = UIcons.reveal;
    expect(
      reveal,
      const UIconData(0xe939, uFontFamily: _kIconsFontFamily),
    );
    const UIconData right_arrow_inline_button =
        UIcons.right_arrow_inline_button;
    expect(
      right_arrow_inline_button,
      const UIconData(0xe93a, uFontFamily: _kIconsFontFamily),
    );
    const UIconData save_button = UIcons.save_button;
    expect(
      save_button,
      const UIconData(0xe93b, uFontFamily: _kIconsFontFamily),
    );
    const UIconData search = UIcons.search;
    expect(
      search,
      const UIconData(0xe93c, uFontFamily: _kIconsFontFamily),
    );
    const UIconData security = UIcons.security;
    expect(
      security,
      const UIconData(0xe93d, uFontFamily: _kIconsFontFamily),
    );
    const UIconData setting = UIcons.setting;
    expect(
      setting,
      const UIconData(0xe93e, uFontFamily: _kIconsFontFamily),
    );
    const UIconData settings_logout = UIcons.settings_logout;
    expect(
      settings_logout,
      const UIconData(0xe93f, uFontFamily: _kIconsFontFamily),
    );
    const UIconData swap_camera = UIcons.swap_camera;
    expect(
      swap_camera,
      const UIconData(0xe940, uFontFamily: _kIconsFontFamily),
    );
    const UIconData tablet = UIcons.tablet;
    expect(
      tablet,
      const UIconData(0xe941, uFontFamily: _kIconsFontFamily),
    );
    const UIconData tap_select_box = UIcons.tap_select_box;
    expect(
      tap_select_box,
      const UIconData(0xe942, uFontFamily: _kIconsFontFamily),
    );
    const UIconData unencrypted = UIcons.unencrypted;
    expect(
      unencrypted,
      const UIconData(0xe943, uFontFamily: _kIconsFontFamily),
    );
    const UIconData unmute = UIcons.unmute;
    expect(
      unmute,
      const UIconData(0xe944, uFontFamily: _kIconsFontFamily),
    );
    const UIconData video_call_button = UIcons.video_call_button;
    expect(
      video_call_button,
      const UIconData(0xe945, uFontFamily: _kIconsFontFamily),
    );

    const UIconData video_play = UIcons.video_play;
    expect(
      video_play,
      const UIconData(0xe94e, uFontFamily: _kIconsFontFamily),
    );
  });
}
