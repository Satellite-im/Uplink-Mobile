name: ui_showroom
description: UI Library Showroom.
version: 0.0.1
homepage:
publish_to: none

environment:
  sdk: ">=2.17.6 <3.0.0"
  flutter: "3.0.5"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.2
  ui_library:
    path: '../ui_library'

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

flutter:
  assets:
    - images/
